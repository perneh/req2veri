[pytest]
testpaths =
    suite_00_empty
    suite_10_functional
    suite_99_load
python_files = test_*.py
addopts = -ra
markers =
    load: bulk tree test (~15k HTTP calls at default scale; run only when needed: pytest -m load)
